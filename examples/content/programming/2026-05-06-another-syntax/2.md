А так то тут код на gleam

# Оператор case

```gleam
import gleam/int
import gleam/io

pub fn main() {
  let x = 5
  let result = case x {
    n if n > 0 -> "Positive"
    0 -> "Zero"
    _ -> "Negative"
  }
  io.debug(result)
}```
